# symlang
Node client for Sym. Requires the core: `pip install sym-lang`
```js
import { sym } from "symlang";
const Calc = await sym.java("java.util.ArrayList");
```
Publish: copy clients/node/sym.mjs here, `npm publish`.
